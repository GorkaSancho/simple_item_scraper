# Web Scraper

Generic scraper that extracts links, names and prices from items in a web and exports them to a CSV

# Use

python scraper.py

Enter the URL when the program tells you

The selector looks for `a[href*="/item/"]` by default, you can change this by modifing the code, depending on what you need to scrap.


